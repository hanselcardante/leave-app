interface Window {
  AutomatedLeave: {
    images: {
      icon: string;
    },
    user: {
      first_name?: string;
      last_name?: string;
      email: string;
      password?: string;
    }
  };
}
